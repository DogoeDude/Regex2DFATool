The Earlier Version is the traditional version to which it doesn't have dead states.
The Recent Version is the updated version to which now has dead states.
The First Version is was the discontinued one due to foreseen flaws from the algorithms provided. 

    Changes:
            Earlier Version:
                a. Better Transitions
                b. Overall better usability
            Recent Version:
                a. Added the ability to create dead states for the visualization.
                b. Lesser transition effect.
            First Version:
                a. No transitions due to it being PDF.
                b. The components for the GUI are dismembered compared to the other versions.

For GUI:
    Earlier and Recent Version utilizes  + GraphViz.
    First Version Utilizes GraphViz

To run the Programs use:
    cd/Enter the Folder version
    python gui.py